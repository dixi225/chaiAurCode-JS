const name ="Harsh"
const repoCount=20
console.log(`${name} have ${repoCount} repositiries on Github`);
const game= new String('Hakuna Matata')
console.log(game.__proto__);        
/*
    COMMON METHODS OF STRING LIKE
        -charAt()
        -indexOf()
        -substring()
        -trim()
        -toLowerCase()
        -toUpperCase()
        -__proto__
        -etc.
*/